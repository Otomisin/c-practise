// Numbers:

let lenght = 23;
let weight = 7.5;

// Strings
let color = "Green";
let lastName = "Tosin"

// booleans
let y = true;
let x = false;

// Objects
const family = {firstname : "Tosin",lastname: "Orenaike"}

// Array Objects 
let food = ["apple", "orange", "mango"]

// Date Objects 
const date = new Date ("2022-03-25")

// Print the data values
console.log(typeof(date))

// Implicit conversion
let age = 5;
let stringS = "10";
let result = age + stringS ; // result will be "510" because str is converted to a string and concatenated with num.

// Explicit conversion
let str = "10";
let num = Number(str); // num will be 10 because str is converted to a number.


let number = 10;
let outcome = String(number); // str will be "10" because num is converted to a string.


