---
title: "RPractise"
author: "Oluwatosin Orenaike"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}

# package2install = c("tidyverse", "sf", "ggplot2", "janitor", "units", "ggtext")
# install.packages(package2install)

library(tidyverse)
library(ggtext)
library(ggplot2)
library(janitor)
library(sf)
library(units)
library(stringr)

```
```{r}
longitude <- c(-116.7, -120.4, -116.7, -113.5, -115.5, -120.8, -119.5, -113.7, -113.7, -110.7)
latitude <- c(45.3, 42.6, 38.9, 42.1, 35.7, 38.9, 36.2, 39, 41.6, 36.9)

line_v1 = cbind(longitude[1:5], latitude[1:5])
line_v2 = cbind(longitude[1], latitude[1])

line1v2 = rbind(line_v1, line_v2)
line1v2list = list(line1v2)
lineploy = st_polygon(line1v2list)


linv2 = st_linestring(line)


linesf = st_linestring(cbind(longitude[1:5], latitude[1:5]))

st_is_valid(linesf)
```
```{r}
# Combine the first five pairs of coordinates
bindsf <- cbind(longitude[1:5], latitude[1:5])

# Close the polygon by adding the first point at the end
# bindsfr <- rbind(bindsf, c(longitude[1], latitude[1])) #or

bindf2 = cbind(longitude[1], latitude[1])

bindsfr = rbind(bindsf, bindf2)

bindsfrlist <- list(bindsfr)

bindsfrlistpol <- st_polygon(bindsfrlist)
```
```{r}
fruits = list ("apple", "Banana", "Mango")
fruits[-2]


```


```{r}

```

