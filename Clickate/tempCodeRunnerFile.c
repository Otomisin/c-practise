/*
Arithmetic Operators
*/

#include <stdio.h>
#include <string.h>

int main (){
     //Arithmetic operators

     // + (addition)
     // - (subtraction)
     // * (Multiplications)
     // / (Division)
     // % (modulus)
     // ++ (increment)
     // -- (decrement)

     int x = 5;
     int y =2;

     int z = x + y

     printf("The result is %d," z);
     return 0
}