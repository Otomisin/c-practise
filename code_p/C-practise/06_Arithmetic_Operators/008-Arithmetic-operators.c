/*
Arithmetic Operators
*/

#include <stdio.h>
#include <string.h>

int main (){
     //Arithmetic operators

     // + (addition)
     // - (subtraction)
     // * (Multiplications)
     // / (Division)
     // % (modulus)
     // ++ (increment)
     // -- (decrement)

     int x = 5;
     int y =2;

     // int z = x + y;
     // int z = x - y;
     // int z = x*y;
     // float z = (float)x / (float)y; // There is a need to cast the divsor as a float 
     // int z = x % y;
     // x++;
     // x--;

     // printf("The result is %f", z);
     //printf("The result is %d", z);
     printf("The result is %d", x);
     
     return 0;
}